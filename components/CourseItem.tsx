import { CourseItem as CourseItemType } from '@/types';
import React from 'react';

type CourseItemProps = {
  course: CourseItemType;
};
export const CourseItem = () => {
  return <div>PreviewPostItem</div>;
};
